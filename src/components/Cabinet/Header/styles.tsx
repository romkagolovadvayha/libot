import React from 'react';
import styled from "@emotion/styled";

const ST = {
    Container: styled.footer`
        background: #fff;
    `,
    Child: styled.div`
      background: #fff;
    `,
};
export default ST;